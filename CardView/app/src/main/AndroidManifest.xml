<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cardview"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CardView">
        <activity android:name=".recycleview.recyclercard.muhsya.MuhsyaActivity">
        </activity>
        <activity android:name=".recycleview.recyclercard.ErthuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".recycleview.another.sekolahmobile.MangaActivity" />
        <activity android:name=".recycleview.another.PratamawijayaActivity" />
        <activity android:name=".recycleview.RecyclerviewMain" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>